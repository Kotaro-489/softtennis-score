# ADR-004 端末内SQLite保存

## 決定

試合記録はSQLiteへ保存し、試合ごとのポイントイベント履歴をJSONとして保持する。

## 理由

試合中のオフライン利用と自動再開を優先する。イベント履歴から再計算するため、取消・分析・将来のJSON書出しで値の整合性を保てる。

## 影響

ViewModelは `MatchRepository` のみを参照する。スキーマ変更にはバージョン付きマイグレーションと移行テストを追加する。
